/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: true,
  experimental: {
    serverActions: true,
  },
};

module.exports = nextConfig;

// mdxRs: true,
// serverComponentsExternalPackages: ["mongooes"],
